/** Official NYC Small Business Services logo (cropped to the green mark, transparent PNG). */
import logoUrl from '../assets/sbs-logo.png';

export function SbsLogo() {
  return <img className="sbslogo" src={logoUrl} alt="NYC Small Business Services" />;
}
